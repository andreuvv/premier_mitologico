## Setup rapido

- Node.js requerido: `>= 20.19.0` (recomendado: `22.x` LTS)
- Instalar dependencias: `npm ci`
- Levantar entorno dev: `npm run dev`

Si usas `nvm`:

```bash
nvm install
nvm use
npm ci
npm run dev
```

